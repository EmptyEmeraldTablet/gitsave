# Gitsave TUI 设计草案

## 设计目标

1. **对标 lazygit 的交互体验**  
   - 多面板布局：左侧为路线/分支，右侧为历史和工作区状态。
   - 快捷键体系：`j/k` 与方向键移动，`tab` 切换焦点，`r` 刷新，`q` 退出。
   - 实时反馈：刷新时同步显示 autosave 配置、当前路线、未提交变更。

2. **统一 autosave 与可视化**  
   - 在 TUI 顶部展示 autosave 配置（启用/间隔/上次保存）。
   - 将 autosave 触发逻辑（未来的 watcher）托管给 TUI 事件循环，CLI 仅负责配置。

3. **状态模型**  
   - `routes`: 对应 `Git2Core::list_routes()`，提供过滤历史的依据。
   - `history`: `Git2Core::get_history()` 的结果，并记录真实路线信息。
   - `status`: `Git2Core::get_status()` 输出，用于工作区面板。
   - `autosave`: `ConfigManager::load_auto_save_config()`，用于 UI 角标与提示。
   - `focus`: 表示当前操作焦点（路线列表或历史列表），影响按键行为。

4. **事件循环**  
   - 基于 `crossterm` + `ratatui`，200ms tick。
   - `poll` 事件响应键盘；`Instant` 控制定期刷新（默认 10 秒）。
   - 结构上与 lazygit 的 UI loop 保持一致（raw mode + alternate screen）。

## 布局

```
┌───────────────────────────────────────────────────────────┐
│ Header: 路径 / Autosave 状态 / 提示                       │
├───────────────┬───────────────────────────────────────────┤
│ Routes Panel  │ History Panel                             │
│ (30%)         │ (70% 上半区)                              │
├───────────────┼───────────────────────────────────────────┤
│ Autosave Info │ Status & Pending Changes (70% 下半区)      │
└───────────────┴───────────────────────────────────────────┘
```

## 按键映射

| 键        | 行为                                             |
|-----------|--------------------------------------------------|
| `q`       | 退出 TUI                                         |
| `r`       | 强制刷新数据                                     |
| `Tab`     | 切换焦点（路线 ↔ 历史）                         |
| `↑ / k`   | 焦点面板上移                                     |
| `↓ / j`   | 焦点面板下移                                     |
| `PageUp`  | 历史列表快速上移                                 |
| `PageDown`| 历史列表快速下移                                 |

## 后续扩展

1. **Autosave 触发**：在事件循环中检测距离上次保存的时间，满足配置后调用 `SaveManager::save` 并刷新历史。
2. **操作面板**：类似 lazygit 的右下角面板，为未来的 `save/load/tag` 操作保留空间。
3. **通知系统**：增设 toast/日志区域，显示最近的 save/load/compare 结果。

当前实现阶段聚焦基础骨架：多面板渲染、焦点切换、列表导航、状态展示，为后续实时 autosave 与交互式操作打下基础。
